<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HealthApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.HealthApp" parent="Base.Theme.HealthApp" />
    <style name="MyRatingBar" parent="Widget.AppCompat.RatingBar">
        <!--non selected star color-->
    <item name="colorControlNormal">#4d000000</item>

        <!-- selected star color-->
        <item name="colorControlActivated">@color/dialog_color1</item>
    </style>
</resources>